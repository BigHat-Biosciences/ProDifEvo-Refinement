biopython
biotite
blosum
fair-esm
lmdb
matplotlib
mdanalysis
numpy<2
pandas
pdb-tools
requests
scikit-learn
seaborn
sequence-models
torch_geometric
#torch-scatter
tqdm
wandb
mdtraj
tmtools
omegaconf
dm-tree
ml_collections
deepspeed
anarci

# ---- AlphaFold2 backend (replaces ESMFold for structure-based reward) ----
# colabdesign provides the mk_afdesign_model API and bundles its own copy of
# alphafold; pinned to the same commit used by mber-open for compatibility.
colabdesign @ git+https://github.com/sokrypton/ColabDesign.git@d024c4e
# JAX must match the colabdesign-pinned version; flax is constrained for the
# same reason. Use jax[cuda12] on a CUDA host; on CPU/macOS swap to plain jax.
jax==0.5.2
flax<0.10.0
# Required for AF2 PDB output / atom utilities.
dm-tree
ml_collections

# ---- Optional: NanoBodyBuilder2 binder pre-folding for AF2 templating ----
# Only needed when running with --use_template. Provides ImmuneBuilder.NanoBodyBuilder2.
ImmuneBuilder